<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/>
    <stroke android:width="2dp" android:color="@android:color/black"/> <!-- Border màu đen -->
    <corners android:radius="8dp"/> <!-- Tùy chọn: Bo góc nếu cần -->
</shape>
